import React from "react";

const Alert = () => {
  return (
    <div style={{}} className="alert">
      <h4 style={{ fontWeight: "bold", color: "white", padding: "20px" }}>
        Word not found.Try Again!!
      </h4>
    </div>
  );
};

export default Alert;
